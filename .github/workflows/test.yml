# This is a basic workflow to help you get started with Actions

name: CI
on:
  schedule:
    - cron: '5 4 * 6 *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Remove File
        uses: JesseTG/rm@v1.0.0
        with:
          # The file path to remove
          path: /docs/_data/gmdmar-facets-daily/gmdmar-facets-.json
